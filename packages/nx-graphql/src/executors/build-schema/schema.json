{
  "$schema": "http://json-schema.org/schema",
  "title": "BuildSchema executor options",
  "type": "object",
  "properties": {
    "schemaFile": {
      "type": "string",
      "description": "Output path for the .graphql file, relative to workspace root"
    },
    "resolversModule": {
      "type": "string",
      "description": "Path to module exporting a GraphQLSchema array of resolver classes"
    },
    "project": {
      "type": "string",
      "description": "NX project name (optional, defaults to current project context)"
    }
  },
  "required": ["schemaFile", "resolversModule"]
}
